services:
  api:
    ports:
      - "5000:5000"
    volumes:
      - ./flask-app:/app
    environment:
      - FLASK_DEBUG=1
      - FLASK_APP=app.py
    command: ["flask", "run", "--host=0.0.0.0", "--port=5000", "--debug"]
